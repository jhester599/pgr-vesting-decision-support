[pytest]
testpaths = tests
addopts = --tb=short
markers =
    integration: marks tests as integration (slow, real I/O)
    slow: marks intentionally expensive tests that can be skipped with --fast
    unit: fast, no I/O
