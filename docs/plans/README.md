# Legacy Plans

This directory contains legacy v8-v34 Codex execution plans.

These files are retained for auditability because older closeouts and archived
research summaries reference them. They are not the active location for new
implementation plans.

For current-era plans, use:

- `docs/superpowers/plans/`

For version closeouts and handoffs, use:

- `docs/closeouts/`
