# Operations logs

`fetch_status.md` is appended by `scripts/initial_fetch.py` (default
`--status-file`), run by the dispatch-only `initial_fetch_prices.yml` and
`initial_fetch_dividends.yml` workflows.

Moved here from `data/fetch_status.md` in v180.
